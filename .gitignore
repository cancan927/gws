# 1. 编译产物：应用程序和插件二进制文件
# 由 'go build' 生成，不同系统和架构下有不同的后缀
*.exe
*.exe~
*.dll
*.so
*.dylib

# 2. 测试与性能分析输出
# 测试二进制文件
*.test
# 测试覆盖率输出 (由 'go test -coverprofile=c.out' 生成)
*.out
# 性能分析文件 (由 'go test -cpuprofile', '-memprofile' 等生成)
*.prof
# 竞态检测输出 (例如 'race.out')
race.out

# 3. 依赖管理
# 模块的本地缓存目录 (go mod download 会将依赖缓存到这里，但不应提交)
# 注意：这是你电脑的全局缓存，不是项目的一部分
# 如果项目内有 vendor 目录，且你决定不使用它，则取消注释下一行
# vendor/
# Go 工作区文件 (用于本地多模块开发，CI 和其他开发者环境不应依赖它)
go.work
go.work.sum

# 4. 编辑器/IDE 配置文件
# VSCode
.vscode/
# JetBrains IDEs (GoLand, IntelliJ)
.idea/
*.iml
# Vim
*.swp
*.swo
*~
# Emacs
*#
.#*
\#*\#
/.dir-locals.el

# 5. 操作系统生成的文件
# macOS
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# 6. 其他常见临时文件和目录
# 日志文件
*.log
# 临时备份文件
*.bak
*.tmp
*.old
# 你自定义的临时目录 (例如用于存放调试数据的 ./tmp/)
tmp/
# 本地调试用的临时文件 (例如 main_debug.go)
*_debug.go

# 7. cgo 相关中间文件 (如果项目中使用了 cgo)
# 这些文件由 cgo 工具生成，不应手动管理或提交
**/*.cgo1.go
**/*.cgo2.c

# 8. 如果你使用 GoLand 的缓存目录 (可选，但建议)
**/go-build-cache/
